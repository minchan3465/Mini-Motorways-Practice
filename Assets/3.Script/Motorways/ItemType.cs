using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Motorways {
    public enum ItemType {
        Road,           // 도로
        TrafficLight,   // 신호등 (예정)
        Roundabout,     // 원형 교차로 (예정)
        Motorway        // 고속도로 (예정)
    }
}
